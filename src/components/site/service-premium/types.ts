import type { LucideIcon } from "lucide-react";

export type ServiceIconItem = {
  icon: LucideIcon;
  title: string;
  detail?: string;
};

export type ServiceHeroCopy = {
  titleFirst: string;
  titleAccent: string;
  subtitle: string;
};
